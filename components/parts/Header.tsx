import { FC } from "react";

const Header: FC = () => {
  return (
    <header>
      <div className="container mt-12">
        <h2>This is an header</h2>
      </div>
    </header>
  );
};

export default Header;
